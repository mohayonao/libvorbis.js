/* interpolated lookup based cos function, domain 0 to PI only */
function vorbis_coslook(a) {
  NOT_IMPLEMENTED();
}

/* interpolated 1./sqrt(p) where .5 <= p < 1. */
function vorbis_invsqlook(a) {
  NOT_IMPLEMENTED();
}

/* interpolated 1./sqrt(p) where .5 <= p < 1. */
function vorbis_invsq2explook(a) {
  NOT_IMPLEMENTED();
}

/* interpolated lookup based fromdB function, domain -140dB to 0dB only */
function vorbis_fromdBlook(a) {
  NOT_IMPLEMENTED();
}
