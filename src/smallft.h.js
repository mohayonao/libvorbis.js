function drft_lookup(p) {
  p = p||{};
  
  // int n;
  // float *trigcache;
  // int *splitcache;
  
  p.n = 0;
  p.trigcache = null;
  p.splitcache = null;
  p.__name = "drft_lookup";
  
  return p;
}
